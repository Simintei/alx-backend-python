apiVersion: apps/v1
kind: Deployment
metadata:
  name: django-messaging-deployment
  labels:
    app: django-messaging
spec:
  replicas: 3
  selector:
    matchLabels:
      app: django
      version: blue

      app: django-messaging
 template:
    metadata:
      labels:
        app: django-messaging
    spec:
      containers:
        - name: django
          image: your-dockerhub-username/django-app:2.0  # ← updated version
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
=======
        - name: django-messaging-container
          image: your_dockerhub_username/django-messaging-app:latest
          ports:
            - containerPort: 8000
          env:
            - name: DJANGO_SETTINGS_MODULE
              value: "messaging_app.settings"
            - name: DEBUG
              value: "1"
          # Optional: If your app connects to a DB service inside cluster
            - name: DATABASE_URL
              value: "postgres://user:password@db-service:5432/messagingdb"

---
apiVersion: v1
kind: Service
metadata:
  name: django-messaging-service
spec:
  selector:
    app: django-messaging
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
>>>>>>> Stashed changes
